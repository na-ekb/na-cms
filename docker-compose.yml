services:
    na-cms.test:
        build:
            context: ./.docker/8.2
            dockerfile: Dockerfile
            args:
                WWWGROUP: '${WWWGROUP}'
        extra_hosts:
            - 'host.docker.internal:host-gateway'
        ports:
            - '${APP_PORT:-80}:80'
            - '${VITE_PORT:-5173}:${VITE_PORT:-5173}'
        environment:
            WWWUSER: '${WWWUSER}'
            LARAVEL_SAIL: 1
            XDEBUG_MODE: '${SAIL_XDEBUG_MODE:-off}'
            XDEBUG_CONFIG: '${SAIL_XDEBUG_CONFIG:-client_host=host.docker.internal}'
            IGNITION_LOCAL_SITES_PATH: '${PWD}'
        volumes:
            - '.:/var/www/html'
            - './storage/logs/cron:/var/log/cron'
        networks:
            - na-cms.sail
        depends_on:
            - na-cms.mysql
            - na-cms.redis
    na-cms.mysql:
        image: 'mysql/mysql-server:8.0'
        ports:
            - '${FORWARD_DB_PORT:-3306}:3306'
        environment:
            MYSQL_ROOT_PASSWORD: '${DB_PASSWORD}'
            MYSQL_ROOT_HOST: '%'
            MYSQL_DATABASE: '${DB_DATABASE}'
            MYSQL_USER: '${DB_USERNAME}'
            MYSQL_PASSWORD: '${DB_PASSWORD}'
            MYSQL_ALLOW_EMPTY_PASSWORD: 1
        volumes:
            - 'na-cms.sail-mysql:/var/lib/mysql'
            - './vendor/laravel/sail/database/mysql/create-testing-database.sh:/docker-entrypoint-initdb.d/10-create-testing-database.sh'
        networks:
            - na-cms.sail
        healthcheck:
            test:
                - CMD
                - mysqladmin
                - ping
                - '-p${DB_PASSWORD}'
            retries: 3
            timeout: 5s
    na-cms.redis:
        image: 'redis:alpine'
        ports:
            - '${FORWARD_REDIS_PORT:-6379}:6379'
        volumes:
            - 'na-cms.sail-redis:/data'
        networks:
            - na-cms.sail
        healthcheck:
            test:
                - CMD
                - redis-cli
                - ping
            retries: 3
            timeout: 5s
#    na-cms.ngrok:
#        image: ngrok/ngrok:latest
#        restart: unless-stopped
#        command:
#            - "start"
#            - "--all"
#            - "--config"
#            - "/etc/ngrok.yml"
#        volumes:
#            - "./ngrok.yml:/etc/ngrok.yml"
#            - "./storage/logs/ngrok.log:/var/log/ngrok.log"
#        networks:
#            - na-cms.sail
#        ports:
#            - 4040:4040
networks:
    na-cms.sail:
        driver: bridge
volumes:
    na-cms.sail-mysql:
        driver: local
    na-cms.sail-redis:
        driver: local
